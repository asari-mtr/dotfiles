# fpath
# fpath=(~/.npm-zsh-completion ${fpath})

# alias
alias e="subl"
alias ls="ls -G -w"

# brewfile wrap
# if [ -f $(brew --prefix)/etc/brew-wrap ];then
#   source $(brew --prefix)/etc/brew-wrap
# fi
